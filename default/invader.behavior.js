var dispatcher = {
    
    run: function() {
        for(var name in Game.creeps) {
            var creep = Game.creeps[name];
            
            if(!creep.memory.role) {
               
            }
        }
    }
}

module.exports = dispatcher;